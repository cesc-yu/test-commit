# first commit

```cmd
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/cesc-yu/test-commit.git
git push -u origin master

// 拉取代码合并：failed to push some refs to git
git pull --rebase origin master
```
